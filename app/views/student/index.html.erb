<script>
$(document).on('change', '#FilePickerInput', function () {
	var input = $(this), numFiles = input.get(0).files.length, label = input.val();
	input.trigger('fileselect', [numFiles,label]);
});

$(document).ready(function () {
    $('#FilePickerInput').on('fileselect', function (event, numFiles, label) {
        var input = $(this).parents('#FilePickerGroup').find('#FilePickerTextBox'), text = label;
        if (numFiles > 1) {
            alert('Please submit only 1 file.');
            input.val("");
        } else {
        	input.val(text);
        }
    });
});

$(document).on('click', '#SubmitButton', function () {
	var input = $('#FilePickerInput'), numFiles = input.get(0).files.length, label = input.val();
	if (numFiles == 1) {
		alert('File Submitted');
		document.getElementById("CurrentSubmission").innerHTML = label;
	}
});

</script>

<div class="container-fluid">

	<!-- View Instructions -->
	<div class="row">
		<div class="col-lg-12">
			<button type="button" class="btn btn-info" data-toggle="modal" data-target="#InstructionsModal">View Instructions</button>
		</div>
	</div>
	
	<!-- Modal -->
	<div id="InstructionsModal" class="modal fade" role="dialog">
		<div class="modal-dialog">

		  <!-- Modal content-->
		  <div class="modal-content">
		    <div class="modal-header">
		    	<button type="button" class="close" data-dismiss="modal">&times;</button>
		      <h4 class="modal-title">Assignment Instructions</h4>
		    </div>
		    <div class="modal-body">
		      <object type="application/pdf" data="http://www.inkwelleditorial.com/pdfSample.pdf" width="100%" height="500">this is not working as expected</object>
		    </div>
		    <div class="modal-footer">
		      <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
		    </div>
		  </div>
		  
		</div>
	</div>
	
	</br>
	
	<!-- Upload File -->
	<div class="row">
		<div class="col-lg-6">
			<div class="input-group" id="FilePickerGroup">
				<span class="input-group-btn">
					<span class="btn btn-primary btn-file" id="FilePickerButton">
					  Browse 
					  <input type="file" multiple="" id="FilePickerInput"></input>
					</span>
				</span>	
				<input type="text" class="form-control" id="FilePickerTextBox"></input>
			</div>
		</div>
		<div class="col-lg-6">
			<button type="button" class="btn btn-warning" id="SubmitButton">Submit</button>
		</div>
	</div>
	
	</br>
	
	<!-- Current Submission -->
	<div class="row">
		<div class="col-lg-2">
			<p>CurrentSubmission: </p>
		</div>
		<div class="col-lg-2">
			<a id="CurrentSubmission"></a>
		</div>
		<div class="col-lg-8">
		</div>
	</div>
	
	</br>
	
	<!-- Grade Button -->
	<div class="row">
		<div class="col-lg-12">
			<button type="button" class="btn btn-success">Grade</button>
		</div>
	</div>
	
	</br>
	
	<!-- View Results -->
	<div class="row">
		<div class="col-lg-12">
			<button type="button" class="btn btn-info">View Results</button>
		</div>
	</div>
	
	</br>
	
</div>


